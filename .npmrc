# Flat node_modules (like npm) so Next.js output-file-tracing + OpenNext can
# follow transitive deps (e.g. @swc/helpers). pnpm's default symlinked layout
# breaks the standalone trace and leaves modules out of the Lambda bundle.
node-linker=hoisted
